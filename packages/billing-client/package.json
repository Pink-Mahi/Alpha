{
  "name": "@alpha/billing-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'billing-client TODO (M1)' && exit 0",
    "typecheck": "echo 'billing-client TODO (M1)' && exit 0",
    "test": "echo 'billing-client TODO (M1)' && exit 0",
    "lint": "echo 'billing-client TODO (M1)' && exit 0"
  }
}
