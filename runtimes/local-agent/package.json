{
  "name": "@alpha/local-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir dist --target bun",
    "dev": "bun --hot src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint TODO' && exit 0"
  },
  "dependencies": {
    "@alpha/agent-protocol": "workspace:*",
    "hono": "^4.6.10",
    "playwright": "^1.62.1",
    "postgres": "^3.4.9",
    "yaml": "^2.9.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "^1.1.13",
    "vitest": "^4.1.10"
  }
}
