{
  "name": "@alpha/agent-protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema/*": "./schema/*"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "codegen": "bun run scripts/codegen.ts",
    "validate": "bun run scripts/validate.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
